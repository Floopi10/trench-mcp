{
  "name": "trench-mcp",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Read-only Robinhood Chain market intelligence tools for AI agents.",
  "bin": {
    "trench-mcp": "bin/trench-mcp.mjs"
  },
  "files": [
    "bin",
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "start": "node bin/trench-mcp.mjs",
    "check": "node --check bin/trench-mcp.mjs && node --check src/server.mjs && node --check src/tools.mjs && node --check src/worker.mjs && node --check docs/app.js && node --check docs/terminal.js && node --check docs/connect.js && node --test",
    "inspect": "npx @modelcontextprotocol/inspector node bin/trench-mcp.mjs",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "wrangler": "^4.131.0"
  }
}
