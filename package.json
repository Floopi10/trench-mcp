{
  "name": "trench-mcp",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Read-only Robinhood Chain market intelligence tools for AI agents.",
  "bin": {
    "trench-mcp": "bin/trench-mcp.mjs"
  },
  "scripts": {
    "start": "node bin/trench-mcp.mjs",
    "check": "node --check bin/trench-mcp.mjs && node --check src/server.mjs && node --check src/tools.mjs && node --test",
    "inspect": "npx @modelcontextprotocol/inspector node bin/trench-mcp.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.6.2"
  }
}
